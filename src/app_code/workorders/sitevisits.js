/**
 * Class for submitting finished site visits
 */


import SiteVisit from './sitevisit';

export default class SiteVisits {
    constructor() {

    }

    submit(workorder) {
        if (workorder != null) {
            workorder.certifications.forEach(function(certification) {
            })
        }
    }
}